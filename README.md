# Java-chapter7-Assignment2

Problem Statement
Handle NumberFormat Exception.For example the statement int num=Integer.parseInt ("XYZ"); 
would  throw NumberFormatException because String cannot be parsed to int. So handle it.
